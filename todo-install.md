1. makepkg fails in root, need to get around that to get yay workin in install.sh
2. rofi shouldn't be in install, check other softwares as well. Don't need it in sway
3. Switch pcmanfm to thunar
4. Fix .config copy so it doesn't create a ~/.config/.config
